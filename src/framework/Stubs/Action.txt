<?php

declare(strict_types=1);

namespace Actions;

use Domains\{{className}} as {{className}}Domain;
use Responders\{{className}} as {{className}}Responder;
use TetherPHP\framework\Http\Response;
use TetherPHP\framework\Interfaces\ActionInterface;
use TetherPHP\framework\Requests\Request;

class {{className}} extends Action implements ActionInterface
{
    public function __construct(protected Request $request)
    {
        $this->domain = new {{className}}Domain();
        $this->responder = new {{className}}Responder($request);
    }

    public function __invoke(): Response
    {
        $data = $this->domain->handle();

        return $this->respond($data);
    }
}
