<?php

declare(strict_types=1);

namespace Responders;

use Domains\Results\{{className}} as {{className}}Result;
use TetherPHP\framework\Http\Response;

class {{className}} extends Responder
{
    /**
     * The one place this feature's view variables are named. Everything the
     * template reads is decided here, out of a result the domain owns.
     */
    public function __invoke({{className}}Result $result): Response
    {
        return $this->view('pages.{{viewName}}.index', [
            'title' => $result->title,
        ]);
    }
}
