<?php

declare(strict_types=1);

namespace Domains\Results;

use TetherPHP\framework\Interfaces\DomainResult;

/**
 * What the {{className}} domain returns.
 *
 * Name these properties for the domain, not for the template. The Responder
 * decides what a view is given, so a variable renamed in a view must never
 * reach back into here.
 */
final readonly class {{className}} implements DomainResult
{
    public function __construct(
        public string $title,
    ) {
    }
}
